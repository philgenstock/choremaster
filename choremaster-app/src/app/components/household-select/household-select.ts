import { Component } from '@angular/core';

@Component({
  selector: 'app-household-select',
  imports: [],
  template: '',
  styleUrl: './household-select.scss',
})
export class HouseholdSelect {
  // This component is deprecated and no longer used
  // Household selection is now done through the dashboard
}
